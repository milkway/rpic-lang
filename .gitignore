# Rust
/target
target/
**/*.rs.bk

# Node / web
node_modules/
web/dist/

# OS / editor
.DS_Store

# R extendr vendor artifacts (regenerated by rextendr::vendor_pkgs())
bindings/r/src/rust/vendor/
bindings/r/src/rust/vendor.tar.xz
bindings/r/src/rust/vendor-config.toml
bindings/r/src/*.so
bindings/r/src/*.o
bindings/r/src/rust/target/

# scratch
*.tmp
packaging/dist/

# Claude Code (local AI orientation + memory; not committed)
CLAUDE.md
.claude/
